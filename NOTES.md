# C# Architecture

Steps
Create plan
Create solution structure
    .sln(x) for complex projects with nesting
    Uses dotnet new <TEMPLATE> 
        App, Api, Core, Application, Infrastructure, Tests

Add project references

Add NuGet Packages

Create domain model